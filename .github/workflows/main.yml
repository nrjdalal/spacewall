name: Create Pull Request on Successful Run

on:
  push:
    branches-ignore:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  create-pull-request:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0 # Fetch all history to ensure we have enough data for comparison

      - name: Set up Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Fetch all branches
        run: git fetch --all

      - name: Check for changes in .github or README.md
        id: check_changes
        run: |
          # Check for changes between the current branch and main branch
          if git merge-base --is-ancestor origin/main ${{ github.ref_name }}; then
            CHANGED_FILES=$(git diff --name-only origin/main...${{ github.ref_name }})
          else
            # Fallback to check for changes from the last commit on the current branch
            CHANGED_FILES=$(git diff --name-only HEAD~1)
          fi
          echo "Changed files: $CHANGED_FILES"
          if echo "$CHANGED_FILES" | grep -qE '^\.github|README\.md'; then
            echo "Changes detected in .github or README.md. Skipping build and pull request creation."
            echo "skip_build_and_pr=true" >> $GITHUB_ENV
          else
            echo "skip_build_and_pr=false" >> $GITHUB_ENV
          fi

      - name: Cache dependencies
        if: env.skip_build_and_pr == 'false'
        uses: actions/cache@v4
        with:
          path: |
            ~/.bun
            ${{ github.workspace }}/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/bun.lock') }}-${{ hashFiles('**/*.js', '**/*.jsx', '**/*.ts', '**/*.tsx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/bun.lock') }}-

      - name: Setup Bun
        if: env.skip_build_and_pr == 'false'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Test build
        if: env.skip_build_and_pr == 'false'
        run: bun i && bun run next build

      - name: Create pull request
        if: env.skip_build_and_pr == 'false'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "${{ github.ref_name }} branch changes"
          committer: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
          author: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
          branch: ${{ github.ref }}
          base: main
          title: "${{ github.ref_name }} branch changes"
          body: |
            This pull request was created automatically by GitHub Actions.
